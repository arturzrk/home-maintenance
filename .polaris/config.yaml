vcs:
  type: git
agents:
  available:
  - claude
  selection:
    strategy: preferred
    preferred_implementer: claude
    preferred_reviewer: claude
paths:
  workspace: "backend/src/"
  tests: "backend/tests/"
  deliverables: "polaris-specs/001-property-job-step/contracts/"
  documentation: "docs/"
